---
phase: 2.5
plan: 5
title: "Documentation + Cleanup"
wave: 4
depends_on: [3]
requirements: [MIG-01]
files_modified:
  - CLAUDE.md
  - AGENTS.md
  - app/CLAUDE.md
  - tests/CLAUDE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
---

# Plan 05: Documentation + Cleanup

## Goal
Update all documentation to reflect the Supabase → Firebase migration. Ensure no stale references to Supabase, SQLModel, or PostgreSQL remain in docs.

## must_haves
- CLAUDE.md and AGENTS.md reflect Firebase architecture
- Codebase docs (STACK.md, ARCHITECTURE.md) updated
- App and test CLAUDE.md files updated
- No stale Supabase/SQLModel references in any documentation
- Final verification: `grep -r "supabase\|SQLModel" app/ scripts/ --include="*.py"` returns nothing

<tasks>

<task id="1" title="Update CLAUDE.md">
- Architecture diagram: `core/` description from "config, database engine/session management" → "config, Firebase client management"
- Models line: "Pydantic schemas + SQLModel ORM tables" → "Pydantic schemas + Firestore document models"
- "Pydantic & SQLModel" section → "Pydantic & Firestore":
  - Remove "Always specify `__tablename__`"
  - Update "DB failures log warnings" → "Firestore write failures log warnings"
  - Add: "Document IDs are strings (Firestore auto-generated)"
</task>

<task id="2" title="Update AGENTS.md">
- Same architecture diagram update as CLAUDE.md
- Same Models line update
- Same "Pydantic & SQLModel" → "Pydantic & Firestore" section update
</task>

<task id="3" title="Update app/CLAUDE.md">
- Update Models section: "Pydantic for API schemas + Firestore document validation" (was "Pydantic for API schemas + SQLModel ORM tables")
- Remove `__tablename__` reference
- Update Repositories description: mention Firestore collections instead of SQL tables
- Update Core description: "Firebase client management" instead of "database engine/session management"
</task>

<task id="4" title="Update tests/CLAUDE.md">
- Update `test_db` fixture description: "Firestore-emulator-backed" (was "Supabase-backed")
- Update mock patch paths if changed
- Update test pattern examples: `int` → `str` IDs
- Note: `FIRESTORE_EMULATOR_HOST=localhost:8080` required for test runs
</task>

<task id="5" title="Update .planning/codebase/STACK.md">
- Replace Supabase ecosystem entries:
  - Remove: `supabase >=2.3.0`, `sqlmodel >=0.0.16 + SQLAlchemy JSON type`
  - Add: `firebase-admin >=6.0.0` — Firebase Admin SDK for Firestore access
- Update "Configuration" section: `.env.example` now has Firebase vars
- Update framework list: remove SQLModel, keep Pydantic
</task>

<task id="6" title="Update .planning/codebase/ARCHITECTURE.md">
- Core Infrastructure Layer: "Firebase client singleton" instead of "lazy Supabase client singleton"
  - `app/core/supabase.py` → `app/core/firebase.py`
  - "Depends on: env vars and Firebase Admin SDK" (was "Supabase SDK")
- Domain Model Layer: "Pydantic models" only (remove SQLModel tables reference)
- Schema Setup Script Entry: `scripts/seed_firebase.py` (was `scripts/setup_supabase_schema.py`)
  - "Create Firestore collections and seed sample project/material/BOM records"
- State Management: Remove reference to Supabase client singleton
</task>

</tasks>

## Verification
```bash
grep -r "supabase" app/ scripts/ --include="*.py" | grep -v __pycache__  # Should return nothing
grep -r "SQLModel" app/ --include="*.py" | grep -v __pycache__  # Should return nothing
grep -ri "supabase" CLAUDE.md AGENTS.md app/CLAUDE.md tests/CLAUDE.md  # Should return nothing
```

## Success Criteria
- [ ] No "supabase" references in CLAUDE.md, AGENTS.md, app/CLAUDE.md, tests/CLAUDE.md
- [ ] No "SQLModel" references in CLAUDE.md or AGENTS.md
- [ ] STACK.md lists firebase-admin, not supabase/sqlmodel
- [ ] ARCHITECTURE.md references firebase.py, not supabase.py
- [ ] Architecture diagrams show "Firebase client management" in core layer
