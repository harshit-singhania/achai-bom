---
phase: 2.5
plan: 4
title: "Tests"
wave: 4
depends_on: [3]
requirements: [MIG-01]
files_modified:
  - tests/conftest.py
  - tests/test_repositories.py
  - tests/test_async_jobs.py
  - tests/test_status_endpoint.py
  - tests/test_setup_supabase_schema.py (delete)
autonomous: true
---

# Plan 04: Tests

## Goal
Update all test files to use Firestore emulator instead of Supabase, fix ID type assertions, and remove irrelevant Supabase-only tests.

## must_haves
- Test fixtures use Firestore emulator (`localhost:8080`)
- All ID type assertions check `str` not `int`
- Test isolation via `reset_firestore_client()` and collection cleanup
- Supabase-only test file deleted
- All tests pass against Firestore emulator

<tasks>

<task id="1" title="Rewrite tests/conftest.py">
- Remove: `sqlalchemy`, `sqlmodel`, `app.core.database` imports
- Replace `test_db` fixture: Firestore emulator cleanup (delete all docs from all collections: `projects`, `floorplans`, `materials_pricing`, `generated_boms`, `async_jobs`)
- Set `FIRESTORE_EMULATOR_HOST=localhost:8080` in fixture setup
- Use `reset_firestore_client()` from `app.core.firebase` for isolation between tests
- Keep all PDF-related fixtures unchanged
</task>

<task id="2" title="Update tests/test_repositories.py" depends_on="1">
- `assert isinstance(result, int)` → `assert isinstance(result, str)`
- `get_*_by_id(99999)` → `get_*_by_id("nonexistent_id")`
- Remove SQLModel/SQLAlchemy imports
- Replace `from app.core import database as db_module` with firebase imports
- Update any mock patch paths from `app.core.database` to `app.core.firebase`
</task>

<task id="3" title="Update tests/test_async_jobs.py" depends_on="1">
- ID type assertions: `int` → `str`
- Nonexistent IDs: `999999` → `"nonexistent_id"`
- "Rogue job_type" test: insert directly via Firestore client instead of SQLAlchemy
- Remove `from app.core import database as db_module`
- Update mock patch paths as needed
</task>

<task id="4" title="Update tests/test_status_endpoint.py" depends_on="1">
- Remove `from app.core import database as db_module`
- `/api/v1/status/99999` → `/api/v1/status/nonexistent_id`
- `"99999"` → `"nonexistent_id"` in assertions
- Update any mock patch paths
</task>

<task id="5" title="Delete tests/test_setup_supabase_schema.py">
Delete entirely — all 7 tests are Supabase URL parsing, completely irrelevant to Firestore.
</task>

</tasks>

## Verification
```bash
FIRESTORE_EMULATOR_HOST=localhost:8080 pytest tests/test_repositories.py -v
FIRESTORE_EMULATOR_HOST=localhost:8080 pytest tests/test_async_jobs.py -v
FIRESTORE_EMULATOR_HOST=localhost:8080 pytest tests/test_status_endpoint.py -v
```

## Success Criteria
- [ ] `tests/conftest.py` has no SQLAlchemy/SQLModel/Supabase imports
- [ ] All ID assertions check `str` type
- [ ] All nonexistent ID tests use string IDs
- [ ] `tests/test_setup_supabase_schema.py` is deleted
- [ ] All test files import from `app.core.firebase` where needed
- [ ] Tests pass with `FIRESTORE_EMULATOR_HOST=localhost:8080`
